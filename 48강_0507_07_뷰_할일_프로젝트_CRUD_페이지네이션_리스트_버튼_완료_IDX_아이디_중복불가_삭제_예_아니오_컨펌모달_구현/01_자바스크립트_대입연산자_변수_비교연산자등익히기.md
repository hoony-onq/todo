```JS
    // = 대입연산자  => 변수 a 에게 숫자 0(Zero)를 넣는다.(저장한다.)

    const a = 0;  // Number 타입(Type 자료(데이터)형)
    const b ='0'; // String 타입(Type 자료(데이터)형)

    // 비교 연산자 =>  반환값은 true(1) 또는 false(0) 인 논리 값으로 반환한다.
    //  >   초과
    //  <   미만
    //  >=  크거나 같다 이상
    //  <=  작거나 같다 이하
    //  ==  같다
    //  === 같다  권장
    //  !== 같지 않다  부정(Not)

    // 또는     or   =>  ||   파이프 기호(Symbol 심볼)
    // 그리고   and  =>  &&   앰퍼센드 기호(Symbol 심볼)
    // 부정     Not  =>  !==  
    
    // false => 숫자 0 과 같다 
    // true =>  숫자 1 과 같다 

    console.log(  a == b );  // true 반환 왜?  단순비교 버그(Bug 오류)
    console.log(  a === b ); // false  논리값 타입까지 문자열과 숫자 까지 비교 된다.  정확


    // 로컬 저장소에 할일리스트 키 속성이 없다면 (null 이면) 아무것도 안한다.
    // 로컬 저장소에 할일리스트 키 속성이 있다면 (null 아니면) this.state.할일리스트 에 저장한다.

    // 객체를 문자열 방식으로 변경하는 방법은 JSON.stringify(객체) => 데이터 타입  String 문자열
    // 문자열을 객체방식으로 변경하는 방법은 JSON.parse(문자열) =>  데이터 타입 Object()  객체
    if(localStorage.getItem('할일리스트') !== null) {
        this.state.할일리스트 = JSON.parse(localStorage.getItem('할일리스트'));
    }


    localStorage.setItem('할일리스트', JSON.stringify(this.state.할일리스트))

    // 영구 저장소(스토레이지 로컬스토레이지) => 계속 저장 유지한다.                    
    // 저장 방식 반드시 문자열로 저장해야한다.
    // 객체를 문자열 방식으로 변경하는 방법은 JSON.stringify(객체) => 데이터 타입  String 문자열
    // 문자열 객체방식으로 변경하는 방법은 JSON.parse(문자열) =>  데이터 타입 Object()  객체
    // localStorage.setItem('키속성 key', 문자열로저장(키밸류value))

```