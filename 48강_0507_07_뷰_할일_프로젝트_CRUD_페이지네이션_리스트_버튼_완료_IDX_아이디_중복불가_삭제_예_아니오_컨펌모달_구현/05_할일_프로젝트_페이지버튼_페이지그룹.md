# 할일 프로젝트 현재 페이지버튼 페이지네이션
1. 현재페이지 버튼 표시
2. 다음페이지 이동
3. 이전페이지 이동
4. 처음페이지 이동
5. 마지막페이지 이동
6. 오류 예외처리 try{} catch(err){}
7. 버튼페이지네이션 구현
   알고리즘 => 
```js

<template v-for="i, idx in state.pages">
    <button 
        @click.prevent="onClickPageBtn(i)" 
        :class="{'on': i===page }"  
        v-if="idx>=state.btnStart && idx<state.btnEnd"                          
    >
        {{i}}
    </button>
</template>


<script>
    data(){
        return {
                        // watch 가 페이지를 감시하도록 page 변수를 리턴문에 바로 설정한다.
            page: 1,  // 현재페이지 번호 => 로딩시  또는 페이지 이동시 현재페이지 번호 
            state: {
                // 입력 & 수정데이터 상태변수
                할일: '',      // 새로운 입력 데이터
                기한: '',      // 오늘 날짜 시간 초기값 설정
                수정: false,    // 수정함 true, 수정안함 fasle
                idx: '',         // 수정할 아이디가 저장되어있다. 
                
                할일리스트: [], // 누적 저장되는 데이터 배열    
                
            
                // 페이지네이션 전체 상태변수
                total: 0,     // 전체 리스트 갯수
                pages: 0,     // 페이지수 => 페이지 버튼 수 를 결정한다.                   

                // 리스트 페이지네이션 상태변수
                listShow: 10,  // 한화면의 보여줄 리스트 갯수
                listStart: 0,
                listEnd: 0,

                // 버튼 페이지네이션 상태변수
                btnShow: 5,  // 한화면의 보여줄 버튼 갯수
                btnStart: 0,
                btnEnd: 0,
            }
        }
    }

    created(){ // 로딩시 실행 끝 또는 새로고침시 실행
        // 8. 버튼 페이지네이션 시작번호 0, 끝번호 4  0~<5,   5~<10 ... 10~<15 ....

        page  1  2  3  4  5   0 ~  5미만
        page  6  7  8  9 10   5 ~ 10미만
        page 11 12 13 14 15  10 ~ 15미만
        page 16 17 18 19 20  10 ~ 15미만

        this.state.btnStart = (Math.ceil(this.page/this.state.btnShow)-1) * this.state.btnShow
        this.state.btnEnd   = this.state.btnStart + this.state.btnShow
    },
    methods: {
            // 페이지버튼 클릭 이벤트 => 페이지이동
            onClickPageBtn(num){

                // 현재페이지 버튼 번호
                this.page = num;  // 페이지 이동시 변경

                // 리스트 페이지네이션
                this.state.listStart = num-1
                this.state.listStart = this.state.listStart * this.state.listShow
                this.state.listEnd   = this.state.listStart + this.state.listShow

                // 버튼 페이지네이션
                this.state.btnStart = (Math.ceil(this.page/this.state.btnShow)-1) * this.state.btnShow
                this.state.btnEnd   = this.state.btnStart + this.state.btnShow

            },
    }    

<script>
```
8. idx 고유번호 중복허가 안함 

9. 삭제 => 예 / 아니오

10. 닷홈 배포

11. 깃허브 배포


